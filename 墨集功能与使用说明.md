# 墨集桌面工作台

## 详细功能与使用手册

## 产品简介

墨集是一款 Windows 桌面工作台，用于集中管理本地资料、编辑 Office 文档、处理 Excel 数据，并通过 AI 助理完成日常工作。文件默认保存在本机；AI 是否联网取决于当前配置的模型服务。

## 主要功能

### 文件与资料

- 扫描本地文件夹并建立文件库；支持搜索、类型筛选、标签、收藏、最近使用和集合。
- 右键文件可加入集合、重命名、移除索引或打开所在文件夹。
- 支持 DOCX、PDF、PPTX、XLSX、CSV、TXT 和常见图片格式预览。

### Excel 工作区

- 工作表标签切换、冻结表头、单元格编辑和公式显示。
- 点击列标（A、B、C）选择整列，点击行号（1、2、3）选择整行；按住拖动可连续选择多行或多列。
- 拖动行列分隔线调整大小，支持放大和缩小；右键菜单提供插入、删除、合并和取消合并。
- 支持格式刷，将字体、颜色、边框、对齐方式和数字格式复制到其他区域。
- 公式结果由本地计算引擎计算，支持 SUM、AVERAGE、COUNT、COUNTA、MIN、MAX、四则运算及常用条件统计。

### AI 助理

- 直接在右侧对话框用自然语言提出任务，AI 会结合当前文件、工作表和选区理解上下文。
- AI 只生成结构化操作计划，实际计算和文件写回在本地完成。
- 修改后显示影响区域、修改前内容、公式、计算结果和操作说明；支持“知道了”清除提示及撤销本次修改。

## 从打开文件到完成修改

1. 双击 `moji-desktop.exe` 启动，或运行安装包完成安装后从开始菜单打开。
2. 在文件库扫描或选择文件夹，双击目标文件。
3. 对 Excel 文件选择工作表，再点击目标单元格、行号、列标或拖选区域。
4. 可直接编辑单元格；按 Enter 确认，按 Esc 放弃本次输入。
5. 也可在右侧 AI 对话框描述任务。发送前确认当前工作表和选区上下文正确。
6. 协助模式下检查计划后点击确认；自主模式会直接执行。完成后视图自动刷新。
7. 如需恢复，点击“撤销本次 AI 修改”。

## AI 指令示例

- `销售明细 B2:B20 求和，结果写入 B21`
- `把 C2:C20 的平均值写到 C21，并保留公式`
- `按“部门”列统计“金额”列平均值，写入右侧汇总区域`
- `将 A2 的格式刷到 A3:A10`
- `把当前选中的列宽调到 120 像素`

- `将 A2 的格式刷到 A3:A10，并保持原内容不变`
- `在“汇总”工作表 B2 写入“销售明细”D2:D100 的总和`

描述越具体越可靠，建议包含“工作表名称 + 源区域 + 目标单元格 + 期望结果”。目标不明确时，AI 会要求补充信息，不会猜测写入位置。

## 权限与安全

- **建议模式**：只分析，不写入文件。
- **协助模式**：展示目标区域、公式和结果，用户确认后写回。
- **自主模式**：直接写回，但保留操作记录和撤销版本。
- 写回前会校验文件版本；文件被其他程序修改时会拒绝覆盖。
- 写回失败不会显示成功提示，原文件保持不变。

## 常见问题

**AI 一直等待**：检查网络和模型服务配置；可先切换为建议模式重试。等待期间切换页面不会丢失任务状态。

**看不到高亮**：确认已重新打开最新程序；写回成功后重新进入文件即可看到影响区域。

**文件无法写回**：关闭 Excel 等占用文件的程序，确认文件有写入权限，并检查是否发生版本冲突。

**公式显示暂不支持**：未知函数会原样保留并提示，不会擅自改写原公式。

## 其他工作台功能

### 任务与专注

在“工作台”中可以创建任务、设置优先级和截止日期，使用专注计时器记录工作时段；任务状态、专注时长和今日摘要会自动汇总。计时器支持开始、暂停、继续和结束，结束后可在历史记录中查看。

### 笔记与知识

可创建 Markdown 笔记，使用标签和集合组织内容。笔记支持搜索、预览和继续编辑；文件库中的文档可加入集合，集合只保存引用，不会复制或移动原始文件。

### 音乐与环境

工作台提供专注音乐控制：播放、暂停、切换曲目和音量调节。音乐状态与专注计时相互独立，切换页面不会中断播放。

### AI 上下文与历史

AI 会读取当前打开文件、工作表、活动单元格、选区和最近对话作为上下文。每次请求、操作计划、写回结果和错误都会保存到当前会话历史；切换功能后返回，未完成的分析仍会继续并恢复显示。

### 设置与服务状态

在设置中可选择 AI 服务提供商、模型和权限模式，并查看本地核心服务状态。服务不可用时仍可使用文件浏览、预览和本地 Excel 编辑；AI 请求会显示明确的错误原因，不会伪造完成结果。

## 键盘与鼠标操作

- Enter：确认单元格或发送 AI 消息（输入框中可使用 Shift+Enter 换行）。
- Esc：取消当前单元格编辑或关闭菜单。
- Ctrl/Cmd+Z：撤销最近一次本地编辑；AI 修改建议使用“撤销本次 AI 修改”。
- 点击并拖动表头：选择连续行或列；点击空白区域：解除表头选择。
- 拖动行列分隔线：调整行高或列宽，向任意方向拖动均可。

## 数据与隐私

墨集不会将本地文件复制到项目目录。文件库索引和会话记录保存在本机应用数据目录；删除索引不会删除原文件。发送给 AI 服务的内容仅限请求所需的上下文，使用第三方模型时请遵守相应服务的隐私政策。

## 开发者运行（开源仓库）

环境要求：Windows 10/11、Node.js 24、pnpm 11、Rust stable 和 Visual Studio C++ 构建工具。

```text
pnpm install
pnpm dev              # 启动开发界面
pnpm build            # 类型检查和前端生产构建
pnpm test             # 前端测试
pnpm test:rust        # Rust 测试
pnpm build:desktop    # 构建 Windows 桌面程序和安装包
```

仓库不提交 `node_modules`、`dist`、`src-tauri/target`、本地密钥或安装包；这些内容均可由上述命令重新生成。提交问题时请附系统版本、复现步骤和日志中的错误信息，并移除个人文件路径及密钥。

## 交付文件

- `src-tauri/target/release/moji-desktop.exe`：免安装可执行程序。
- `src-tauri/target/release/bundle/nsis/墨集_0.1.0_x64-setup.exe`：Windows 安装包。

首次使用建议先复制一份重要的 Excel 文件进行试用。应用会在写回前保留撤销所需的原始版本。
